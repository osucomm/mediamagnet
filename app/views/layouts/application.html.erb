<%= render 'shared/head' %>

<body>
  <%= render 'shared/osu_navbar' %>
  <%= render 'shared/navbar' %>

  <div id="page-content" class="container">
    <div class="messages">
    <% flash.each do |key, value| %>
      <%= render 'shared/message', flash_type: key, message: value %>
    <% end %>
    </div>

    <%= yield %>

  </div>

  <%= render 'shared/footer' %>

</body>
</html>
